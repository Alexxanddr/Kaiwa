apiVersion: v1
kind: Service
metadata:
  name: kaiwa
  labels:
    app.kubernetes.io/name: kaiwa
spec:
  selector:
    app.kubernetes.io/name: kaiwa
  ports:
    - name: http
      port: 8130
      targetPort: http
  type: ClusterIP